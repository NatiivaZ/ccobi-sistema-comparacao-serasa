"""PORTFOLIO — motor de comparacao omitido."""
from portfolio_omitted import omit


def analisar_bases(*args, **kwargs):
    omit("analisar_bases — cruzamento SERASA x Divida Ativa")


def _construir_mapa_modal(*args, **kwargs):
    omit("_construir_mapa_modal")
